# REForum 前端

REForum 论坛系统的 React 前端应用。

## 功能特性

- 🏠 首页帖子列表展示
- 📝 帖子发布和编辑
- 💬 评论和回复功能
- 👤 用户认证和资料管理
- 🏷️ 版块和标签分类
- 📱 响应式设计
- 🎨 现代化 UI 设计

## 技术栈

- **React 18** - UI 框架
- **React Router 6** - 路由管理
- **Axios** - HTTP 客户端
- **Vite** - 构建工具
- **React Icons** - 图标库
- **date-fns** - 日期处理

## 开发

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

应用将在 http://localhost:3000 启动

### 构建生产版本

```bash
npm run build
```

### 预览生产构建

```bash
npm run preview
```

## 环境变量

创建 `.env` 文件：

```env
VITE_API_BASE_URL=http://localhost:8080/api/v1
```

## 项目结构

```
frontend/
├── src/
│   ├── components/      # 可复用组件
│   │   ├── Header.jsx   # 顶部导航栏
│   │   ├── Sidebar.jsx  # 左侧导航栏
│   │   ├── RightSidebar.jsx  # 右侧边栏
│   │   ├── PostCard.jsx # 帖子卡片
│   │   ├── CommentList.jsx # 评论列表
│   │   ├── LoginModal.jsx  # 登录模态框
│   │   └── RegisterModal.jsx # 注册模态框
│   ├── pages/          # 页面组件
│   │   ├── Home.jsx    # 首页
│   │   ├── PostDetail.jsx # 帖子详情
│   │   ├── UserProfile.jsx # 用户资料
│   │   └── CreatePost.jsx # 创建帖子
│   ├── services/       # API 服务
│   │   └── api.js      # API 客户端
│   ├── context/        # Context API
│   │   └── AuthContext.jsx # 认证上下文
│   ├── App.jsx         # 主应用组件
│   └── main.jsx        # 入口文件
├── package.json
├── vite.config.js
└── Dockerfile
```

## Docker 部署

### 构建镜像

```bash
docker build -t reforum-frontend .
```

### 运行容器

```bash
docker run -p 80:80 reforum-frontend
```

## API 集成

前端通过 Axios 与后端 API 通信。API 基础 URL 通过环境变量配置。

所有 API 请求会自动添加 JWT token（如果用户已登录）。

## 样式

使用 CSS 变量定义主题色彩，便于自定义和主题切换。

主要颜色：
- 主色：`#ff4500` (Orange Red)
- 次色：`#0079d3` (Blue)
- 背景：`#dae0e6` (Light Gray)
- 卡片背景：`#ffffff` (White)

## 浏览器支持

- Chrome (最新版)
- Firefox (最新版)
- Safari (最新版)
- Edge (最新版)

